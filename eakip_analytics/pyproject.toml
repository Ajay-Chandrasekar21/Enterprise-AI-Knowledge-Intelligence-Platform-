[tool.poetry]
name = "eakip_analytics"
version = "1.0.0"
description = "EAKIP Analytics engine placeholder"
authors = ["Antigravity"]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
